fix: corrigir hero image — usar img nativa com object-cover

O HumanizedImage nao funcionava como background full-bleed porque
o wrapper div nao propagava as dimensoes absolutas para o img interno.

- Substituido HumanizedImage por tag img nativa no Hero
- object-cover + object-top garante fill total da viewport
- fetchPriority high para carregamento imediato
- Overlay mais escuro para legibilidade
- text-shadow para legibilidade extra no mobile
- Paddings responsivos ajustados (pt-28 sm:pt-32 pb-24)
- h1 menor no mobile (text-4xl) para nao estourar
- bg-stone-900 como fallback enquanto imagem carrega