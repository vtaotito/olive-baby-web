// Olive Baby Web - Theme exports
export { ThemeProvider, useTheme } from './ThemeProvider';
export { 
  type Theme, 
  getStoredTheme, 
  setStoredTheme, 
  applyTheme, 
  getResolvedTheme,
  initializeTheme,
} from './theme';
